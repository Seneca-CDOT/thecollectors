getType(obj){}										//returns an object's type. Only works for custom objects.
loadXML(filename){}									//does the necessary setup for reading from an xml file. Returns an XML Document object
rng(min,max){}										//simplified rng method
getDotProduct(dx1, dy1, dx2, dy2){}					//returns the dot product of two lines given x and y deltas
segIntersection(x1, y1, x2, y2, x3, y3, x4, y4){}  	//checks if the given 2 lines intersect. Returns the point of intersection